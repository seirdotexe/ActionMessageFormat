/**
 * @module AMF/Options
 *
 * @typedef {object} AMFOptions
 * @property {boolean} compressSparse - Whether or not to compress sparse array entries. Default to true
 */

/** @type {AMFOptions} */
const Options = {
  compressSparse: true
};

export default Options;